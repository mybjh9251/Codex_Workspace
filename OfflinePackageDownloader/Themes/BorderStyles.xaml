<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="PanelBorder" TargetType="Border">
        <Setter Property="Background" Value="White" />
        <Setter Property="BorderBrush" Value="#DFE5EE" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
    </Style>

    <Style x:Key="MockupPanelBorder" TargetType="Border">
        <Setter Property="Background" Value="White" />
        <Setter Property="BorderBrush" Value="#DFE6EF" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
    </Style>

    <Style x:Key="MockupIconTile" TargetType="Border">
        <Setter Property="Width" Value="56" />
        <Setter Property="Height" Value="56" />
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="VerticalAlignment" Value="Top" />
    </Style>

    <Style x:Key="MockupPackageCard" TargetType="Border" BasedOn="{StaticResource MockupPanelBorder}">
        <Setter Property="Padding" Value="20" />
        <Setter Property="Margin" Value="0,0,14,14" />
    </Style>

    <Style x:Key="MockupDependencyChip" TargetType="Border">
        <Setter Property="BorderBrush" Value="#DCE3EC" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="Margin" Value="0,0,10,10" />
    </Style>
</ResourceDictionary>
