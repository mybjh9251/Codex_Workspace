<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="ProviderTabs" TargetType="TabControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <TabPanel IsItemsHost="True" KeyboardNavigation.TabIndex="1" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ProviderTabItem" TargetType="TabItem">
        <Setter Property="MinHeight" Value="68" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Grid Margin="0,0,34,0" Width="Auto">
                        <Border x:Name="Underline"
                                Height="5"
                                CornerRadius="2.5"
                                VerticalAlignment="Bottom"
                                Background="Transparent" />
                        <ContentPresenter ContentSource="Header" Margin="0,0,0,8" VerticalAlignment="Center" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Underline" Property="Background" Value="#1685E5" />
                            <Setter Property="Foreground" Value="#1685E5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
